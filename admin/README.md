# InRage Admin

React (Vite) web panel for gym staff.

## Setup

```bash
npm install
npm run dev
```

Runs on `http://localhost:5173`.

## Status

Scaffolding only. Dashboard shell with InRage visual identity.

## What's next

- Connect to backend (`http://localhost:4000`)
- Members list
- Form to create the daily workout
- Class scheduling
- Staff auth
